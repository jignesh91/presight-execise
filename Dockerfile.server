FROM node:20-alpine

WORKDIR /app

COPY server/package.json server/package-lock.json* ./
RUN npm install

COPY server/ ./

RUN npx tsc

RUN mkdir -p data

EXPOSE 4000

CMD ["sh", "-c", "if [ ! -f data/users.db ]; then node -e \"require('./dist/seed.js')\"; fi && node dist/index.js"]
